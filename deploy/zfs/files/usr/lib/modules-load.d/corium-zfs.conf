# Load the ZFS kernel module at boot.
#
# corium-agent also modprobes it before creating pools, so a first boot does not
# depend on this; it is here so that later boots -- where the pools are imported
# by the zfs-import systemd units rather than by the agent -- have the module
# without waiting for anything else to pull it in.
zfs
