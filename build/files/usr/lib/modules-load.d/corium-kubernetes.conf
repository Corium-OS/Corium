# Kernel modules Kubernetes networking depends on.
#
# br_netfilter must be loaded before the bridge-nf-call sysctls above can be
# applied; loading it here makes the ordering explicit rather than incidental.
br_netfilter
overlay
